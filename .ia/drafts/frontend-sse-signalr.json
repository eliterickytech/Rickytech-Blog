{
  "titulo": "Tunelamento em tempo real no React: SSE e SignalR na prática",
  "lede": "Precisa empurrar dados do servidor para a tela sem ficar batendo em polling? Entenda a diferença entre Server-Sent Events e SignalR e veja como consumir cada um no React, com código real e confirmado na fonte.",
  "categoria": "frontend",
  "tags": [
    "react",
    "sse",
    "signalr",
    "tempo-real",
    "eventsource",
    "websockets",
    "frontend",
    "javascript"
  ],
  "imagem_en": "Abstract 16:9 wide background of glowing horizontal light streams flowing from left to right through a dark tunnel of soft gradient blues and purples, representing real-time data streaming, particles of light forming continuous lines, depth of field, no text, no letters, clean modern digital art",
  "corpo_html": "<p>Interfaces modernas raramente ficam paradas esperando o usu&aacute;rio apertar F5. Notifica&ccedil;&otilde;es, cota&ccedil;&otilde;es, placares, logs de deploy, tokens de um LLM chegando aos poucos &mdash; tudo isso pede que o <strong>servidor empurre</strong> os dados para o navegador assim que eles existem. &Eacute; o que chamo informalmente de \"tunelamento\": abrir um canal por onde o backend joga eventos direto na tela. Neste artigo comparo duas formas maduras de fazer isso no React &mdash; <strong>Server-Sent Events (SSE)</strong> e <strong>SignalR</strong> &mdash; e mostro c&oacute;digo pr&aacute;tico de cada uma.</p>\n\n<h2>Server-Sent Events: streaming unidirecional sobre HTTP</h2>\n<p>SSE &eacute; um padr&atilde;o do pr&oacute;prio navegador. O cliente abre uma conex&atilde;o HTTP persistente com a API <code>EventSource</code>, e o servidor mant&eacute;m essa conex&atilde;o aberta enviando mensagens no formato <code>text/event-stream</code>. &Eacute; um fluxo de <strong>m&atilde;o &uacute;nica</strong>: s&oacute; do servidor para o cliente.</p>\n<p>Dois pontos que a documenta&ccedil;&atilde;o da MDN e do web.dev deixam claros e que fazem o SSE brilhar:</p>\n<ul>\n<li><strong>Reconex&atilde;o autom&aacute;tica</strong>. Se a conex&atilde;o cai, o navegador tenta reabrir sozinho (por volta de 3 segundos por padr&atilde;o). O servidor pode ajustar esse intervalo com o campo <code>retry:</code> e retomar de onde parou usando o <code>id:</code> de cada evento.</li>\n<li><strong>Zero protocolo novo</strong>. Roda sobre HTTP comum, passa por proxies e infraestrutura HTTP sem cerim&ocirc;nia.</li>\n</ul>\n<blockquote>Aten&ccedil;&atilde;o a um limite hist&oacute;rico: sobre HTTP/1.1, os navegadores permitem no m&aacute;ximo ~6 conex&otilde;es SSE simult&acirc;neas por dom&iacute;nio (contando todas as abas). Sob HTTP/2 esse teto sobe bastante, porque as requisi&ccedil;&otilde;es s&atilde;o multiplexadas.</blockquote>\n\n<h3>Consumindo um SSE no React</h3>\n<p>O padr&atilde;o &eacute; criar o <code>EventSource</code> dentro de um <code>useEffect</code> e sempre limpar a conex&atilde;o no retorno. Use <code>onmessage</code> para eventos sem nome e <code>addEventListener</code> para eventos nomeados (o campo <code>event:</code> enviado pelo servidor).</p>\n<pre><code>import { useEffect, useState } from \"react\";\n\nfunction Cotacoes() {\n  const [precos, setPrecos] = useState([]);\n\n  useEffect(() =&gt; {\n    const source = new EventSource(\"/api/cotacoes\");\n\n    // Evento padr&atilde;o (sem campo \"event:\" no servidor)\n    source.onmessage = (e) =&gt; {\n      const dado = JSON.parse(e.data);\n      setPrecos((atual) =&gt; [dado, ...atual].slice(0, 20));\n    };\n\n    // Evento nomeado: servidor envia \"event: alerta\"\n    source.addEventListener(\"alerta\", (e) =&gt; {\n      console.warn(\"Alerta:\", e.data);\n    });\n\n    source.onerror = (err) =&gt; {\n      console.error(\"Falha no EventSource:\", err);\n      // O navegador j&aacute; tenta reconectar sozinho.\n    };\n\n    return () =&gt; source.close(); // fecha ao desmontar\n  }, []);\n\n  return (\n    &lt;ul&gt;\n      {precos.map((p, i) =&gt; (\n        &lt;li key={i}&gt;{p.ativo}: {p.valor}&lt;/li&gt;\n      ))}\n    &lt;/ul&gt;\n  );\n}\n\nexport default Cotacoes;</code></pre>\n<p>Repare que n&atilde;o h&aacute; l&oacute;gica de reconex&atilde;o manual: o <code>EventSource</code> cuida disso. Voc&ecirc; s&oacute; fecha explicitamente com <code>source.close()</code> quando o componente sai de cena.</p>\n\n<h2>SignalR: canal bidirecional com fallback autom&aacute;tico</h2>\n<p>SignalR &eacute; a biblioteca de tempo real do ASP.NET Core. Diferente do SSE, ela &eacute; <strong>bidirecional</strong>: o cliente chama m&eacute;todos no servidor (o \"hub\") e o servidor chama m&eacute;todos no cliente. Por baixo, o SignalR escolhe o melhor <strong>transporte</strong> dispon&iacute;vel &mdash; de prefer&ecirc;ncia <em>WebSockets</em>, com fallback para <em>Server-Sent Events</em> e, por &uacute;ltimo, <em>Long Polling</em>. Ou seja: o SSE pode ser at&eacute; o encanamento interno do SignalR quando WebSocket n&atilde;o est&aacute; dispon&iacute;vel.</p>\n<p>No cliente JavaScript/TypeScript voc&ecirc; usa o pacote oficial <code>@microsoft/signalr</code>:</p>\n<pre><code>npm install @microsoft/signalr</code></pre>\n\n<h3>Conectando a um hub no React</h3>\n<p>Monte a conex&atilde;o com <code>HubConnectionBuilder</code>, registre os handlers com <code>connection.on(...)</code> <strong>antes</strong> de chamar <code>start()</code> (boa pr&aacute;tica recomendada pela Microsoft) e habilite a reconex&atilde;o com <code>withAutomaticReconnect()</code>.</p>\n<pre><code>import { useEffect, useRef, useState } from \"react\";\nimport * as signalR from \"@microsoft/signalr\";\n\nfunction Chat() {\n  const [mensagens, setMensagens] = useState([]);\n  const conexaoRef = useRef(null);\n\n  useEffect(() =&gt; {\n    const connection = new signalR.HubConnectionBuilder()\n      .withUrl(\"/chatHub\")\n      .withAutomaticReconnect() // 0, 2, 10 e 30s por padr&atilde;o\n      .configureLogging(signalR.LogLevel.Information)\n      .build();\n\n    // Servidor chama: Clients.All.SendAsync(\"ReceiveMessage\", user, msg)\n    connection.on(\"ReceiveMessage\", (user, message) =&gt; {\n      setMensagens((atual) =&gt; [...atual, `${user}: ${message}`]);\n    });\n\n    connection.onreconnecting(() =&gt; console.log(\"Reconectando...\"));\n    connection.onreconnected(() =&gt; console.log(\"Reconectado!\"));\n\n    connection\n      .start()\n      .then(() =&gt; console.log(\"SignalR conectado.\"))\n      .catch((err) =&gt; console.error(err));\n\n    conexaoRef.current = connection;\n    return () =&gt; { connection.stop(); };\n  }, []);\n\n  const enviar = async (user, texto) =&gt; {\n    try {\n      // Chama um m&eacute;todo p&uacute;blico do hub no servidor\n      await conexaoRef.current.invoke(\"SendMessage\", user, texto);\n    } catch (err) {\n      console.error(err);\n    }\n  };\n\n  return (\n    &lt;ul&gt;\n      {mensagens.map((m, i) =&gt; (\n        &lt;li key={i}&gt;{m}&lt;/li&gt;\n      ))}\n    &lt;/ul&gt;\n  );\n}\n\nexport default Chat;</code></pre>\n<p>Do lado do cliente h&aacute; duas formas de falar com o servidor: <code>invoke</code>, que retorna uma <code>Promise</code> resolvida com o valor de retorno do m&eacute;todo do hub, e <code>send</code>, que apenas dispara a mensagem sem esperar resposta. Para receber, sempre <code>connection.on(\"NomeDoMetodo\", ...)</code>.</p>\n<blockquote>Sobre reconex&atilde;o: sem par&acirc;metros, <code>withAutomaticReconnect()</code> tenta reconectar aos 0, 2, 10 e 30 segundos e desiste ap&oacute;s quatro falhas. Voc&ecirc; pode passar um array de intervalos ou um <code>IRetryPolicy</code> customizado. E aten&ccedil;&atilde;o: ele n&atilde;o retenta falhas do <em>start()</em> inicial &mdash; isso voc&ecirc; trata na m&atilde;o.</blockquote>\n\n<h2>SSE x SignalR: qual escolher?</h2>\n<table>\n<thead>\n<tr><th>Crit&eacute;rio</th><th>Server-Sent Events</th><th>SignalR</th></tr>\n</thead>\n<tbody>\n<tr><td>Dire&ccedil;&atilde;o</td><td>Unidirecional (servidor &rarr; cliente)</td><td>Bidirecional (cliente &harr; servidor)</td></tr>\n<tr><td>Transporte</td><td>HTTP (<code>text/event-stream</code>)</td><td>WebSockets, com fallback para SSE e Long Polling</td></tr>\n<tr><td>Cliente</td><td>Nativo (<code>EventSource</code>), sem lib</td><td>Pacote <code>@microsoft/signalr</code></td></tr>\n<tr><td>Reconex&atilde;o</td><td>Autom&aacute;tica pelo navegador</td><td><code>withAutomaticReconnect()</code> (opt-in)</td></tr>\n<tr><td>Servidor</td><td>Qualquer stack HTTP</td><td>Acoplado ao ASP.NET Core / Azure SignalR</td></tr>\n<tr><td>Melhor para</td><td>Feeds, notifica&ccedil;&otilde;es, streaming de texto</td><td>Chat, colabora&ccedil;&atilde;o, jogos, RPC em tempo real</td></tr>\n</tbody>\n</table>\n<p>A regra pr&aacute;tica: se o fluxo &eacute; s&oacute; do servidor para a tela e voc&ecirc; quer simplicidade m&aacute;xima sem depender de uma stack espec&iacute;fica, <strong>SSE</strong> resolve com eleg&acirc;ncia &mdash; e sem instalar nada no front. Se voc&ecirc; precisa de <strong>via dupla</strong>, chamadas RPC entre cliente e servidor, grupos, presen&ccedil;a e um degrad&ecirc; robusto de transporte, especialmente num backend .NET, o <strong>SignalR</strong> entrega tudo pronto. N&atilde;o s&atilde;o rivais absolutos: o SignalR inclusive usa SSE como um de seus transportes internos.</p>\n<p>Comece pelo mais simples que atende ao caso de uso. Muita gente sobe um WebSocket completo para o que um humilde <code>EventSource</code> resolveria em dez linhas.</p>\n\n<h2>Fontes</h2>\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events\" rel=\"noopener nofollow\" target=\"_blank\">MDN Web Docs &mdash; Using server-sent events</a></li>\n<li><a href=\"https://learn.microsoft.com/en-us/aspnet/core/signalr/javascript-client\" rel=\"noopener nofollow\" target=\"_blank\">Microsoft Learn &mdash; ASP.NET Core SignalR JavaScript client</a></li>\n<li><a href=\"https://web.dev/articles/eventsource-basics\" rel=\"noopener nofollow\" target=\"_blank\">web.dev &mdash; Stream updates with server-sent events</a></li>\n<li><a href=\"https://blog.logrocket.com/server-sent-events-vs-websockets/\" rel=\"noopener nofollow\" target=\"_blank\">LogRocket &mdash; Server-sent events vs. WebSockets</a></li>\n</ul>",
  "post_id": 224,
  "media_id": 223
}